import { templateHelper } from '@divkitframework/jsonbuilder';
import { RecipeCard } from './RecipeCard';

export const components = {
    RecipeCard,
};

export const helper = templateHelper(components);
